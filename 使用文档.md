# AI 文献检索 Agent 使用文档

这是一个命令行版 AI 文献检索 Agent。v1.0 采用研究工作流架构：`Intake -> Protocol -> Search -> Corpus -> Screening -> Verification -> Synthesis -> Review -> Report`。输入研究需求后，它会自动完成需求解析、研究协议生成、多源检索、语料筛选、证据验证、综合分析、开放 PDF 下载，并输出中英文 APA 风格报告。

## 1. 功能概览

Agent 当前支持：

- 关键词理解：识别关键词是否过宽、歧义或偏离 AI 方向。
- 检索计划：自动生成英文检索式、子方向和检索策略。
- 多源检索：调用 arXiv、Semantic Scholar、OpenAlex、Crossref、OpenReview。
- 研究协议：生成研究问题、纳入/排除标准、检索来源和负面关键词。
- 论文处理：标准化字段、增强去重、排序。
- 语料筛选：把论文分为 `core`、`adjacent`、`exclude`，避免偏题工具进入核心综述。
- 证据验证：记录 DOI、URL、PDF、代码仓库置信度。
- 质量门：输出 `pass`、`retry` 或 `needs_user_attention`。
- 代码链接识别：识别 GitHub、GitLab、Hugging Face、项目页等公开代码链接。
- GitHub 筛选：可只保留有代码链接的论文。
- PDF 下载：只下载明确开放获取的 PDF，不绕过付费墙。
- 双语报告：先生成 `report.canonical.json`，再渲染 `report.zh.md`、`report.en.md`、`report.zh.html`、`report.en.html`、`report.zh.pdf` 和 `report.en.pdf`，保证中英文论文列表一致。
- 综述正文：报告会写出研究背景、主流方法流派、代表论文总结、方法比较、发展趋势和开放问题，而不是只输出论文表格。

## 2. 环境要求

需要 Python 3.10 或更高版本。

在项目根目录运行：

```bash
python --version
```

本地开发安装：

```bash
python -m pip install -e .
```

打包后也可以用 pip 安装 wheel：

```bash
python -m pip install dist/paperpilot-*.whl
```

安装后可以使用两个命令：

```bash
literature-agent --help
PaperPilot --help
paperpilot --help
```

也可以不安装，直接用模块方式运行：

```bash
python -m literature_agent.cli --help
```

## 3. 配置 LLM API

推荐使用配置命令：

```bash
PaperPilot config set --base-url https://api.deepseek.com --model deepseek-chat
PaperPilot config import ./api.json
PaperPilot config show
PaperPilot config list
PaperPilot config use deepseek
```

运行 `config set` 时，如果不传 `--api-key`，会用隐藏输入提示你填写 API Key。

如果第一次直接输入 `PaperPilot`，且没有检测到 LLM 配置，程序会自动进入配置向导。配置会先做一次连通测试，测试通过才保存。未配置可用 LLM 时，PaperPilot 会先暂停使用，并提示你完成配置。

本地文件导入支持如下格式，即使有末尾逗号也会兼容：

```json
{
  "api_key": "sk-...",
  "base_url": "https://api.deepseek.com",
  "model": "deepseek-chat",
}
```

导入后会先测试连通性；测试通过后保存到 `~/.paperpilot/config.json`。之后原始文件可以删除，不影响 PaperPilot 使用。

进入 `PaperPilot` 交互界面后，可以输入：

```text
/model
```

在菜单里管理模型配置：

- 新增或更新模型配置
- 从本地 JSON 文件导入配置
- 切换当前 active 模型
- 删除模型配置
- 测试当前模型

配置文件默认保存到：

```text
~/.paperpilot/config.json
```

在支持的系统上，文件权限会设置为 `600`。

Agent 会按以下顺序读取 LLM 配置：

1. 环境变量 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`
2. 用户级配置 `~/.paperpilot/config.json`
3. 兼容旧配置：当前目录下的 `llmapi.txt`

命令行管理多个模型配置：

```bash
PaperPilot config list
PaperPilot config set --name deepseek --base-url https://api.deepseek.com --model deepseek-chat
PaperPilot config import ./api.json --name deepseek
PaperPilot config set --name openai --model gpt-5.2
PaperPilot config use deepseek
PaperPilot config delete openai
```

在交互界面中管理模型：

```bash
PaperPilot
```

进入后输入：

```text
/model
```

即可新增、切换、删除或测试模型配置。

推荐使用环境变量：

```bash
export OPENAI_API_KEY="你的 OpenAI API Key"
```

也可以把 key 写入项目根目录的 `llmapi.txt`：

```text
sk-...
```

如果使用 OpenAI-compatible 服务，可以写成两行：

```text
https://api.example.com
sk-...
```

也可以写成 key-value 格式：

```text
base_url=https://api.example.com
api_key=sk-...
model=your-model-name
```

当 `base_url` 指向 DeepSeek 且没有显式指定模型时，程序会自动把默认模型从 `gpt-5.2` 切换为 `deepseek-chat`。

如果没有配置可用的 LLM，PaperPilot 会先提示配置。v1.0 不再进入规则 fallback 检索流程，因为需求解析、语料筛选和报告生成都依赖 LLM 才能保持质量。

## 4. 快速开始

### 4.1 交互式聊天入口

安装后直接输入：

```bash
PaperPilot
```

进入后可以用自然语言描述调研需求：

```text
调研RNA逆折叠 序列设计 近五年的文献，要求有代码仓库的
```

PaperPilot 会先解析你的需求，并展示可修改选项：

- 研究主题
- 起始年份
- 论文数量
- 是否要求代码仓库
- 是否下载 PDF
- 多样化检索关键词

确认后再开始正式检索。

如果配置了 LLM，PaperPilot 会在这个阶段把中文需求解析成更适合论文数据库的英文检索词。例如：

```text
调研RNA逆折叠 序列设计 近五年的文献，要求有代码仓库的,方法不限
```

可能会生成：

```text
RNA inverse folding sequence design
RNA sequence design inverse folding
RNA inverse folding algorithm
RNA inverse folding GitHub
RNA sequence design software
RNA inverse folding benchmark
```

你可以在确认菜单里选择“修改检索关键词”，手动增删这些检索式。

### 4.2 命令式入口

检索 `LLM agent`，最多保留 30 篇论文：

```bash
python -m literature_agent.cli "LLM agent" --auto-confirm --max-papers 30
```

检索 `RNA`，由于这个词较宽，建议加 `--auto-confirm` 使用 Agent 推荐的 AI 相关方向继续执行：

```bash
python -m literature_agent.cli "RNA" --auto-confirm --max-papers 50 --since-year 2021
```

只保留找到公开代码链接的论文：

```bash
python -m literature_agent.cli "retrieval augmented generation" --auto-confirm --github-filter required
```

安装后也可以使用项目名命令：

```bash
PaperPilot "retrieval augmented generation" --auto-confirm --github-filter required
```

跳过 PDF 下载，只生成检索结果和报告：

```bash
python -m literature_agent.cli "vision language model" --auto-confirm --no-download
```

指定输出目录：

```bash
python -m literature_agent.cli "AI agent benchmark" --auto-confirm --output-dir runs/agent-benchmark
```

导入本地语料后再补检索：

```bash
PaperPilot "RNA inverse folding sequence design" --auto-confirm --user-corpus ./my_papers --user-corpus references.bib
```

## 5. 常用参数

```bash
python -m literature_agent.cli "关键词" [参数]
```

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `keyword` | 必填 | 要检索的关键词或研究方向。 |
| `--max-papers` | `50` | 最终报告最多保留多少篇论文。 |
| `--since-year` | `2021` | 优先检索该年份之后的论文。 |
| `--output-dir` | 自动生成 | 输出目录；默认生成 `runs/<task-id>`，手动指定时使用该目录。 |
| `--openai-model` | `gpt-5.2` | 用于理解、规划、反思和写报告的模型名。 |
| `--openai-api-key-file` | `llmapi.txt` | API Key 文件路径。 |
| `--unpaywall-email` | 空 | 配置后可通过 Unpaywall 补充开放 PDF 链接。 |
| `--github-filter` | `any` | 控制是否按代码链接筛选论文。 |
| `--auto-confirm` | 关闭 | 关键词有歧义时，自动使用推荐方向继续检索。 |
| `--no-download` | 关闭 | 不下载 PDF，只记录状态。 |
| `--pdf-limit` | 空 | 限制最多下载多少个 PDF。 |
| `--github-search-limit` | `25` | 主动搜索 GitHub 仓库的论文数量。 |
| `--user-corpus` | 空 | 导入本地 PDF、BibTeX、RIS、Markdown 或文本文件/目录；可重复传入。 |
| `--mode` | `apa` | 报告模式：`quick`、`apa`、`systematic`。 |
| `--interaction` | `auto` | 交互模式：`auto` 尽量自动，`gated` 在关键歧义处确认。 |
| `--quality` | `balanced` | 质量门严格度：`fast`、`balanced`、`strict`。 |
| `--include-adjacent` | 关闭 | 在证据矩阵中包含相关但非核心论文。 |

## 6. GitHub 筛选说明

`--github-filter` 有三个取值：

| 取值 | 含义 |
|---|---|
| `any` | 默认值，不过滤论文，只在报告中标注是否找到代码。 |
| `required` | 最终报告视图只保留找到 GitHub、GitLab、Hugging Face 或项目页的论文；完整核心语料仍会保存。 |
| `none` | 只保留没有找到公开代码链接的论文。 |

示例：

```bash
python -m literature_agent.cli "LLM agent" --auto-confirm --github-filter required
```

注意：v1.0 会给代码链接记录置信度，但仍不保证仓库一定可以完整复现论文。

## 7. 关键词理解与确认

当关键词很宽或有歧义时，Agent 会先生成 `query_understanding.md`。

例如输入：

```bash
python -m literature_agent.cli "RNA"
```

Agent 会判断 `RNA` 可能包含：

- RNA biology
- RNA structure prediction
- RNA design
- RNA foundation model
- AI for genomics

默认情况下，它会暂停并询问是否继续。如果你希望自动采用推荐方向，使用：

```bash
python -m literature_agent.cli "RNA" --auto-confirm
```

## 8. 输出文件说明

每次运行会生成一个唯一任务目录，默认在 `runs/<task-id>/` 下，不会因为关键词相同而覆盖旧结果。

任务目录格式类似：

```text
runs/20260513-235812-rna-inverse-folding-a1b2c3/
```

主要文件：

| 文件 | 说明 |
|---|---|
| `task.json` | 任务 ID、关键词、输出目录和关键参数。 |
| `state.json` | 状态机阶段进度。 |
| `manifest.json` | 本次运行生成的文件清单和模型信息。 |
| `events.jsonl` | 阶段事件流，记录 start/progress/warn/error/done 等状态。 |
| `prompt_manifest.json` | Prompt Registry 清单，包括角色、版本和必需 JSON 字段。 |
| `registries.json` | ToolRegistry 与 CapabilityRegistry 清单。 |
| `query_understanding.md` | 对用户关键词的理解、歧义点、推荐检索方向。 |
| `plan.json` | Agent 生成的检索计划，包括检索式、子方向和参数。 |
| `protocol.json` | 研究问题、纳入/排除标准、检索来源、负面关键词。 |
| `metadata.json` | 多源 API 返回的原始标准化论文结果。 |
| `user_corpus_log.json` | 本地用户语料导入日志；跳过文件会记录原因。 |
| `corpus.json` | 全部候选论文的筛选标签、理由和代码证据。 |
| `core_papers.json` | 核心论文语料。 |
| `adjacent_papers.json` | 相关但非核心论文。 |
| `excluded_papers.json` | 被排除论文及原因。 |
| `ranked_papers.json` | 最终报告视图论文列表。 |
| `verification.json` | DOI、URL、PDF、代码仓库置信度验证结果。 |
| `quality_gate.json` | 质量门指标、问题和建议。 |
| `literature_matrix.json` | 论文方法、任务、代码、PDF、局限等证据矩阵。 |
| `synthesis.json` | 方法演进、主题综合、争议点、研究空白。 |
| `evidence_ledger.json` | claim 级证据账本，绑定报告结论与论文引用。 |
| `review_agent_findings.json` | SourceVerifier、RelevanceJudge、CitationCompliance、Devil’s Advocate 的复核结果。 |
| `report.canonical.json` | 中英文报告共用的规范化中间表示。 |
| `reflection.json` | 质量反思结果和最终 verdict。 |
| `download_log.json` | PDF 下载成功、失败、跳过的原因。 |
| `report.zh.md` | 中文调研报告。 |
| `report.en.md` | 英文调研报告。 |
| `report.zh.html` | 中文 HTML 调研报告。 |
| `report.en.html` | 英文 HTML 调研报告。 |
| `report.zh.pdf` | 中文 PDF 调研报告。 |
| `report.en.pdf` | 英文 PDF 调研报告。 |
| `pdfs/` | 下载到的开放 PDF 文件。 |
| `fulltext/` | 成功下载 PDF 的文本抽取结果。 |
| `paper_notes.json` | PDF 全文抽取状态。 |

查看已有任务质量门：

```bash
PaperPilot inspect runs/<task-id>
```

从已有任务目录重新运行：

```bash
PaperPilot resume runs/<task-id>
```

## 9. 推荐工作流

### 9.1 快速了解一个方向

```bash
python -m literature_agent.cli "LLM agent" --auto-confirm --max-papers 30 --no-download
```

适合先看方向背景、代表论文和代码链接，不下载 PDF。

### 9.2 做可复现实验选题

```bash
python -m literature_agent.cli "RAG evaluation" --auto-confirm --github-filter required --max-papers 30
```

适合筛出更可能有代码、可复现实验的论文。

### 9.3 做近年进展调研

```bash
python -m literature_agent.cli "vision language model" --auto-confirm --since-year 2023 --max-papers 50
```

适合聚焦近两三年的最新方法。

### 9.4 控制 PDF 下载数量

```bash
python -m literature_agent.cli "AI agent benchmark" --auto-confirm --pdf-limit 10
```

适合避免一次下载太多 PDF。

## 10. 常见问题

### 没有 LLM API Key 可以用吗？

v1.0 不建议这样使用。没有可用 LLM 配置时，PaperPilot 会提示先配置，因为需求解析、检索词扩展、语料筛选和报告生成都需要 LLM。

### 会下载付费论文吗？

不会。当前只下载明确开放获取的 PDF，例如 arXiv、OpenReview 或 Unpaywall 返回的开放 PDF。

### 为什么有些论文没有 PDF？

可能原因：

- API 没有返回开放 PDF 链接。
- 论文不是开放获取。
- PDF 链接响应失败。
- 使用了 `--no-download`。

具体原因可以看 `download_log.json`。

### 为什么有些论文没有 GitHub？

当前版本主要从论文元数据、摘要、URL 中识别代码链接。如果代码链接只在 PDF 正文、作者主页深层页面或 README 中，可能识别不到。

### 为什么 `RNA` 会要求确认？

因为 `RNA` 是宽泛词，既可以是生物学主题，也可以是 AI for RNA、RNA foundation model、RNA structure prediction 等方向。Agent 会先生成理解文件，避免盲目检索出大量非 AI 文献。

## 11. 开发与测试

运行单元测试：

```bash
python -m unittest discover -s tests
```

运行一个不下载 PDF 的烟测：

```bash
python -m literature_agent.cli "LLM agent" --auto-confirm --max-papers 5 --no-download --output-dir runs/smoke
```

查看输出：

```bash
ls runs/smoke
```

## 12. 打包成 pip 包

项目已经配置为标准 Python 包，包名是 `paperpilot`。

本地开发安装：

```bash
python -m pip install -e .
```

构建 wheel 包：

```bash
rm -rf dist build *.egg-info
python -m pip wheel . -w dist
```

构建完成后，主包会出现在：

```text
dist/paperpilot-1.1.0-py3-none-any.whl
```

安装 wheel：

```bash
python -m pip install dist/paperpilot-1.1.0-py3-none-any.whl
```

安装后可用命令：

```bash
PaperPilot --help
paperpilot --help
literature-agent --help
```

推荐使用 `PaperPilot` 或 `paperpilot`，`literature-agent` 主要用于兼容旧入口。

## 13. 当前限制

- 当前是 CLI 工具，不包含 Web UI。
- 当前不使用 SQLite，历史结果以 JSON 文件保存在每次运行目录中。
- 当前代码链接会记录置信度，但不实际运行仓库。
- 当前 PDF 下载只处理开放链接；成功下载后会抽取文本，但不保证所有 PDF 都能解析完整。
- 当前多源 API 可能受网络、限流、服务稳定性影响。
